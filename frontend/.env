# Development backend URL (for local development)
# REACT_APP_API_URL=http://localhost:8080

# Production backend URL - this is set for the Netlify deployment
REACT_APP_API_URL=https://hospital-dashboard-api.onrender.com

# Important: After deployment, if you change the backend URL,
# go to your Netlify dashboard > Site settings > Environment variables
# and update REACT_APP_API_URL to your deployed backend URL 