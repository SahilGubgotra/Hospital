# Development backend URL - used when running locally
REACT_APP_API_URL=http://localhost:8080

# Production backend URL - uncomment and set this when deploying to Netlify
# REACT_APP_API_URL=https://your-production-backend-url.com

# Important: After deployment, go to your Netlify dashboard > Site settings > Environment variables
# and set REACT_APP_API_URL to your deployed backend URL 